{% extends 'base.html.twig' %}
{% block title %}{{ parent() }}Parrainer un animal{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/parrainage.css') }}">
{% endblock %}
{% block javascripts %}
    <script src="{{ asset('js/parrainage.js') }}"></script>
{% endblock %}
{% block body %}
    <div class="container mt-5">
        <h1 class="text-center text-success display-3 mb-4">🐾 Parrainer un animal</h1>
        <div class="alert alert-success text-center">
            Soutenez nos pensionnaires en devenant parrain ou marraine ! Votre aide contribue à leur bien-être et à la
            préservation des espèces.
        </div>
        <div class="row">
            {% for animal in animaux %}
                <div class="col-md-4 mb-4">
                    <div class="card shadow">
                        <img src="{{ asset('picture/' ~ animal.image) }}" class="card-img-top" alt="{{ animal.nom }}">
                        <div class="card-body">
                            <h5 class="card-title">{{ animal.nom }}</h5>
                            <p class="card-text">{{ animal.descrption }}</p>
                            <a href="{{ path('app_parrainage_add', {'animal': animal.id}) }}"
                               class="btn btn-primary btn-parrainer">PARRAINER</a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}